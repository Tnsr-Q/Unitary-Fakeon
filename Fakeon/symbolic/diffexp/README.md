# symbolic/diffexp

DiffExp configurations for high-precision numerical evaluation of the
PV-projected master integrals along fixed contours.

| File           | Scope                                    |
|----------------|------------------------------------------|
| `system_2D.m`  | 2-D massive Fakeon DE (z, y)             |

Wires into the same residue matrices as `symbolic/hyperint/`.
