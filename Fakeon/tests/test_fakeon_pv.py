"""tests/test_fakeon_pv.py — PV projection regression tests.

Placeholder.  Will host tests that exercise the PV contour deformation and
verify imaginary-part cancellation at the alphabet letters.
"""

from __future__ import annotations


def test_placeholder_pv() -> None:
    # Intentional placeholder: keeps the test module importable so that
    # pytest reports a stable count while the real tests are written.
    assert True
